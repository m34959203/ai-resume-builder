# ---------- FRONT (Vite) ----------
VITE_APP_NAME=AI Resume Builder

# Базовый URL BFF
VITE_API_URL=http://localhost:8000
VITE_API_PREFIX=/api

# Поведение клиента BFF
VITE_USE_MOCKS=false
VITE_API_TIMEOUT_MS=12000
VITE_AREAS_TTL_MS=21600000
VITE_FORCE_KZ=1          # принудительно hh.kz для гео и areas (совместимо с bff.js)

# Dev server / proxy (совместимо с vite.config.js)
VITE_BASE=/
VITE_HTTPS=0             # 1, если у вас есть локальные certs/*
VITE_PROXY_STRIP_PREFIX=0
VITE_PROXY_INSECURE=0
# VITE_RENDER_BFF_URL=    # опционально — если фронт крутится на Render, а бэкенд на другом домене

# PWA (в проде включится автоматически; оставлено для явного управления)
VITE_ENABLE_PWA=1
