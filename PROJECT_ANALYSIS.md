# Глубокий анализ проекта AI Resume Builder

> Дата: 2026-02-06
> Метод: Мультиэкспертный аудит (Архитектор, Frontend-лид, Backend-лид, Security-инженер, QA-лид, DevOps-инженер)

---

## Оглавление

1. [Общая картина](#1-общая-картина)
2. [Архитектор ПО — Архитектурный обзор](#2-архитектор-по)
3. [Frontend-лид — Анализ клиентской части](#3-frontend-лид)
4. [Backend-лид — Анализ серверной части](#4-backend-лид)
5. [Security-инженер — Аудит безопасности](#5-security-инженер)
6. [QA-лид — Тестирование и качество](#6-qa-лид)
7. [DevOps-инженер — Инфраструктура и CI/CD](#7-devops-инженер)
8. [Сводная таблица проблем](#8-сводная-таблица)
9. [Дорожная карта улучшений](#9-дорожная-карта)

---

## 1. Общая картина

**Проект**: AI-powered конструктор резюме с интеграцией HeadHunter и рекомендациями на основе LLM.

**Стек**:
| Слой | Технологии |
|------|-----------|
| Frontend | React 18, Vite 5, Tailwind CSS 3, React Router 6, @react-pdf/renderer |
| BFF | Express 4, Helmet, CORS, express-rate-limit |
| AI | OpenRouter (DeepSeek R1 / Gemma 3) |
| Внешние API | HeadHunter (hh.ru / hh.kz) |
| Деплой | GitHub Pages + Render.com + Docker |

**Метрики кодовой базы**:
- ~6 900 строк фронтенда (26 JSX/JS файлов)
- ~3 400 строк бэкенда (8 JS файлов)
- 0 тестов, 0% покрытия
- 7 уязвимостей npm (4 high, 3 moderate)

---

## 2. Архитектор ПО

### 2.1 Что хорошо

- **BFF-паттерн** — правильное решение. Фронтенд не обращается к внешним API напрямую, секреты на сервере.
- **Honest proxying** — BFF не скрывает ошибки HH API, что упрощает отладку.
- **Дедупликация запросов** — Map-кеш для GET-запросов предотвращает дублирование.
- **Грамотная i18n-архитектура** — LanguageContext + translations.js для 3 языков.
- **PWA** — офлайн-поддержка, service worker, installable.

### 2.2 Критические проблемы

#### P1: Монолитные компоненты (God Objects)
```
src/components/AIResumeBuilder.jsx  — 76 KB
src/components/BuilderPage.jsx      — 54 KB
src/services/bff.js                 — 150+ KB
```
Эти файлы содержат слишком много ответственности. `bff.js` — это одновременно HTTP-клиент, кеш, нормализатор данных, маппер HH API и конструктор запросов.

**Рекомендация**: Декомпозиция по принципу единственной ответственности:
```
services/
  api/client.js          — HTTP-клиент, retry, timeout
  api/cache.js           — GET-кеширование
  hh/jobs.js             — работа с вакансиями
  hh/areas.js            — справочник регионов
  hh/auth.js             — OAuth-поток
  recommendations/api.js — AI-рекомендации
  utils/normalize.js     — нормализация данных
```

#### P2: Отсутствие базы данных
Все данные резюме хранятся в component state и теряются при обновлении страницы. Нет сохранения между сессиями (кроме `app_lang` в localStorage).

**Рекомендация**: Минимально — localStorage/IndexedDB для черновика резюме. Полноценно — Supabase/Firebase для пользовательских данных.

#### P3: Нет слоя абстракции состояния
Используется только `useState` + один Context. При росте проекта это приведёт к prop drilling и сложной отладке.

**Рекомендация**: Zustand или Jotai для глобального состояния (profile, jobs, recommendations).

### 2.3 Архитектурная диаграмма (текущая vs целевая)

**Текущая:**
```
React (монолит) → BFF (Express) → HH API / OpenRouter
     ↓
  localStorage (только язык)
```

**Целевая:**
```
React (модульные компоненты)
  ↓
Zustand (глобальный state)
  ↓
Service Layer (модульный)
  ↓
BFF (Express + валидация + rate limit)
  ↓                    ↓
HH API            OpenRouter
  ↓
IndexedDB / Supabase (персистенция)
```

---

## 3. Frontend-лид

### 3.1 Что хорошо
- Tailwind CSS — единообразные стили без CSS-конфликтов
- Lazy loading компонентов — `React.lazy()` для AIResumeBuilder
- 4 шаблона PDF — хороший набор для MVP
- useMemo/useCallback — попытки оптимизации

### 3.2 Проблемы качества кода

#### P1: Дублирование кода (~20 мест)

Функции `norm()` и `uniqCaseInsensitive()` определены в 3 разных файлах:
- `BuilderPage.jsx:46`
- `AIResumeBuilder.jsx:56`
- `bff.js:686` (inline lambda)

Паттерн дедупликации через `Set` повторяется 15+ раз по всей кодовой базе.

**Рекомендация**: Вынести в `src/utils/strings.js`:
```javascript
export const norm = s => (s || '').trim().toLowerCase();
export const uniqCI = arr => [...new Map(arr.map(s => [norm(s), s])).values()];
```

#### P2: 30+ console.log в продакшн-коде

| Файл | Количество |
|------|-----------|
| `src/services/bff.js` | 23+ |
| `src/components/BuilderPage.jsx` | 3 |
| `src/utils/analytics.js` | 5 |
| `src/App.jsx` | 2 |

**Рекомендация**: Настроить `babel-plugin-transform-remove-console` (уже установлен, но не подключён) или использовать `vite-plugin-remove-console`.

#### P3: Магические числа без констант

- `bff.js:87` — `'12000'` (таймаут)
- `bff.js:90` — `6 * 60 * 60 * 1000` (TTL 6 часов)
- `bff.js:939-968` — скоры: `10, 30, 35, 25, 15, 5, 200, 120, 60, 20`
- `BuilderPage.jsx:843` — `min="14"`, `max="80"` (возраст)

**Рекомендация**: Вынести в `src/constants/index.js`.

#### P4: React-антипаттерны

- `BuilderPage.jsx:462` — пустой `[]` в useCallback при зависимости от `form`
- `BuilderPage.jsx:595-599` — useEffect может менять state, который сам же проверяет → потенциальный infinite loop
- `BuilderPage.jsx:297` — `key={`${s}-${i}`}` — использование индекса в ключах (нарушает reconciliation при reorder)

#### P5: Отсутствие TypeScript

Весь проект на JavaScript без типов. Ни PropTypes, ни JSDoc, ни TypeScript interfaces. При размере `bff.js` в 150+ KB это создаёт серьёзные проблемы поддержки.

**Рекомендация**: Поэтапная миграция начиная с `src/services/` (наиболее критичный слой).

### 3.3 Проблемы UX/Accessibility

- Нет ARIA-атрибутов на интерактивных элементах
- Нет `aria-live` регионов для динамического контента (рекомендации, ошибки)
- Нет skip-to-content ссылки
- Контрастность не проверена
- Нет keyboard navigation для wizard-шагов

---

## 4. Backend-лид

### 4.1 Что хорошо
- Честное проксирование HH API (не скрывает ошибки)
- Health-check эндпоинты (/health, /healthz, /ready, /alive, /version)
- Кеширование HH-запросов (180s TTL)
- Structured logging в server-production.js

### 4.2 Проблемы

#### P1: Три серверных файла с разным поведением

| Файл | Модули | Rate Limit | Helmet | OAuth | Используется |
|------|--------|-----------|--------|-------|-------------|
| `server/index.js` | CommonJS | ❌ Нет | Частично | ❌ | dev |
| `server.js` | ESM | ❌ Нет | ❌ | ❌ | dev proxy |
| `server-production.js` | ESM | ✅ Да | ✅ | ✅ | prod |

**Проблема**: В dev-режиме отключены rate limiting, OAuth, полный Helmet. Поведение dev ≠ prod.

**Рекомендация**: Унифицировать в один серверный entry point с конфигурацией через ENV.

#### P2: Нет валидации входных данных на API

- `/api/recommendations/generate` — профиль может быть 50MB JSON
- `/api/hh/jobs/search` — параметры не валидируются
- `seedSkills` массив может содержать 10 000 элементов

**Рекомендация**: Добавить `express-validator` или `zod` для валидации на уровне route.

#### P3: Отсутствие CSRF-защиты для POST-эндпоинтов

OAuth имеет state-параметр, но все POST-эндпоинты рекомендаций и AI не защищены от CSRF.

#### P4: Раскрытие внутренних деталей в ошибках

```javascript
// server/index.js:450-456
return res.status(r.status).json({
  ok: false,
  error: 'HH_API_ERROR',
  details: errText.slice(0, 2000),  // ← внутренние ошибки HH
  url,                               // ← внутренние URL
});
```

---

## 5. Security-инженер

### 5.1 КРИТИЧЕСКИЕ уязвимости

#### S1: Секреты в git-репозитории (CRITICAL)
Файл `.env` содержит реальные API-ключи и OAuth-секреты:
```
HH_CLIENT_ID=RJSMB82V8R4SR7S6...
HH_CLIENT_SECRET=G3KLD3ODB4OR7PQ9...
OPENROUTER_API_KEY=sk-or-v1-a38450...
```

**Действия**:
1. Немедленно ротировать все ключи
2. Удалить `.env` из git-истории (BFG Repo-Cleaner)
3. Использовать GitHub Secrets / Render Environment Variables

#### S2: Слабый OAuth state (HIGH)
```javascript
// server-production.js:349
const state = Math.random().toString(36).slice(2);
```
`Math.random()` не криптографически безопасен. ~15 бит энтропии вместо минимум 128.

**Исправление**:
```javascript
const crypto = require('crypto');
const state = crypto.randomBytes(32).toString('hex');
```

#### S3: CSP отключён (HIGH)
```javascript
// server/index.js:79
app.use(helmet({ contentSecurityPolicy: false }));
```
Нет защиты от XSS через внедрение скриптов.

### 5.2 Высокие уязвимости

| # | Проблема | Файл | Строка |
|---|---------|------|--------|
| S4 | 7 npm-уязвимостей (включая React Router XSS) | package.json | — |
| S5 | Нет rate limiting в dev-сервере | server/index.js | — |
| S6 | Host-параметр без allowlist | server/routes/hh.js | 15 |
| S7 | Unsafe `window.location.href` без валидации | src/services/bff.js | 330, 339 |
| S8 | Нет лимитов на размер JSON body | server/routes/recommendations.js | — |

### 5.3 Средние уязвимости

- Нет CSRF для POST-эндпоинтов
- Cookies без SameSite в dev-режиме
- Раскрытие внутренних URL в ошибках
- Нет authentication enforcement на публичных API

---

## 6. QA-лид

### 6.1 Текущее состояние тестирования

**Тестов: 0. Покрытие: 0%. Инфраструктура: отсутствует.**

- Нет test runner (Jest/Vitest)
- Нет библиотек для тестирования (React Testing Library)
- Нет E2E-фреймворка (Playwright/Cypress)
- Нет mock-сервера для интеграционных тестов
- `.gitignore` содержит записи для coverage/jest-cache, но файлы не создавались

### 6.2 Рекомендуемая стратегия тестирования

**Фаза 1 — Критические unit-тесты (неделя 1-2):**
```
Инструменты: Vitest + React Testing Library
Цель: покрыть сервисный слой

tests/
  services/
    bff.test.js           — HTTP-клиент, кеш, нормализация
    ai.test.js            — AI-запросы и парсинг
  utils/
    normalize.test.js     — строковые утилиты
    analytics.test.js     — трекинг событий
  server/
    routes/hh.test.js     — HH proxy роуты
    routes/recs.test.js   — рекомендации
    services/ai.test.js   — OpenRouter интеграция
```

**Фаза 2 — Компонентные тесты (неделя 3-4):**
```
BuilderPage.test.jsx      — wizard navigation, form validation
ResumePreview.test.jsx    — рендеринг шаблонов
LanguageSwitcher.test.jsx — переключение языка
ErrorBoundary.test.jsx    — обработка ошибок
```

**Фаза 3 — E2E тесты (неделя 5-6):**
```
Инструмент: Playwright
e2e/
  resume-flow.spec.ts     — полный цикл создания резюме
  job-search.spec.ts      — поиск вакансий
  pdf-export.spec.ts      — генерация PDF
  oauth-flow.spec.ts      — авторизация через HH
```

### 6.3 Метрики качества для внедрения

| Метрика | Текущее | Цель |
|---------|---------|------|
| Unit-тесты | 0 | 80+ |
| Покрытие | 0% | > 70% |
| E2E-сценарии | 0 | 10+ |
| Lint-ошибки | Не измеряется | 0 |
| TypeScript strict | Нет | Да |

---

## 7. DevOps-инженер

### 7.1 Что хорошо
- GitHub Actions для деплоя на GitHub Pages
- render.yaml для декларативного деплоя на Render
- Dockerfile для контейнеризации
- Health-check эндпоинты
- Gzip + Brotli компрессия в сборке

### 7.2 Проблемы

#### D1: CI/CD не запускает тесты и линтер
Текущий workflow `.github/workflows/pages.yml`:
```yaml
- run: npm ci
- run: npm run build
# ← Нет lint, нет тестов, нет проверки типов
```

**Рекомендация**: Добавить в pipeline:
```yaml
- run: npm run lint
- run: npm run typecheck
- run: npm test -- --coverage
- run: npm run build
```

#### D2: Нет staging-окружения
Деплой идёт сразу в production. Нет preview deployments для PR.

#### D3: Нет мониторинга и алертинга
- Нет Sentry/Datadog/New Relic
- Нет error tracking на фронтенде
- Нет метрик latency/error rate на бэкенде
- `src/services/ai.js:41` содержит закомментированный Sentry

#### D4: Нет Dependabot / Renovate
Автообновление зависимостей не настроено. 7 уязвимостей не были обнаружены автоматически.

#### D5: Нет `.nvmrc` / `engines` в корневом package.json
Версия Node не зафиксирована на уровне проекта (только в render.yaml).

---

## 8. Сводная таблица проблем

| # | Проблема | Эксперт | Критичность | Усилие |
|---|---------|---------|-------------|--------|
| S1 | Секреты в git | Security | 🔴 CRITICAL | Низкое |
| S2 | Слабый OAuth state | Security | 🔴 HIGH | Низкое |
| S3 | CSP отключён | Security | 🔴 HIGH | Среднее |
| S4 | npm-уязвимости (7 шт) | Security | 🔴 HIGH | Низкое |
| Q1 | 0 тестов, 0% покрытия | QA | 🔴 HIGH | Высокое |
| A1 | Монолитные компоненты (76KB, 54KB, 150KB) | Архитектор | 🟠 HIGH | Высокое |
| F1 | Дублирование кода (~20 мест) | Frontend | 🟠 MEDIUM | Среднее |
| F2 | 30+ console.log в проде | Frontend | 🟠 MEDIUM | Низкое |
| F3 | Магические числа | Frontend | 🟡 MEDIUM | Среднее |
| F4 | React-антипаттерны | Frontend | 🟡 MEDIUM | Среднее |
| F5 | Нет TypeScript | Frontend | 🟡 MEDIUM | Высокое |
| B1 | 3 разных серверных файла | Backend | 🟠 MEDIUM | Среднее |
| B2 | Нет валидации входных данных | Backend | 🟠 MEDIUM | Среднее |
| B3 | Нет CSRF для POST | Backend | 🟡 MEDIUM | Среднее |
| B4 | Раскрытие внутренних деталей в ошибках | Backend | 🟡 MEDIUM | Низкое |
| A2 | Нет персистентного хранилища | Архитектор | 🟡 MEDIUM | Среднее |
| A3 | Нет state management | Архитектор | 🟡 MEDIUM | Среднее |
| D1 | CI без тестов и линтера | DevOps | 🟠 MEDIUM | Низкое |
| D2 | Нет staging-окружения | DevOps | 🟡 LOW | Среднее |
| D3 | Нет мониторинга (Sentry) | DevOps | 🟡 LOW | Низкое |
| D4 | Нет Dependabot | DevOps | 🟡 LOW | Низкое |
| U1 | Нет ARIA / accessibility | Frontend | 🟡 LOW | Среднее |

---

## 9. Дорожная карта улучшений

### Спринт 1 — Безопасность и стабильность (неотложно)

- [ ] Ротировать все API-ключи (HH_CLIENT_ID, HH_CLIENT_SECRET, OPENROUTER_API_KEY)
- [ ] Удалить `.env` из git-истории
- [ ] `npm audit fix` — закрыть 7 уязвимостей
- [ ] Заменить `Math.random()` на `crypto.randomBytes()` в OAuth state
- [ ] Включить CSP в Helmet
- [ ] Добавить валидацию host-параметра (allowlist: hh.kz, hh.ru)
- [ ] Убрать раскрытие внутренних URL в ошибках

### Спринт 2 — Качество кода

- [ ] Извлечь общие утилиты (norm, uniqCI, dedup) в `src/utils/`
- [ ] Извлечь константы (таймауты, лимиты, скоры) в `src/constants/`
- [ ] Настроить удаление console.log из прод-сборки
- [ ] Исправить React-антипаттерны (useCallback deps, keys)
- [ ] Добавить ESLint + Prettier с общей конфигурацией
- [ ] Добавить `.nvmrc` с Node 18

### Спринт 3 — Тестирование

- [ ] Настроить Vitest + React Testing Library
- [ ] Написать unit-тесты для `services/bff.js` (критический путь)
- [ ] Написать unit-тесты для `server/routes/`
- [ ] Добавить тесты в CI pipeline
- [ ] Цель: > 50% покрытия сервисного слоя

### Спринт 4 — Архитектурный рефакторинг

- [ ] Декомпозировать `bff.js` (150KB → 6-8 модулей)
- [ ] Декомпозировать `BuilderPage.jsx` (54KB → step-компоненты)
- [ ] Декомпозировать `AIResumeBuilder.jsx` (76KB → фичевые модули)
- [ ] Унифицировать серверные файлы (3 → 1)
- [ ] Внедрить Zustand для state management

### Спринт 5 — Персистенция и UX

- [ ] Добавить сохранение резюме в IndexedDB/localStorage
- [ ] Автосохранение черновика
- [ ] Добавить ARIA-атрибуты и keyboard navigation
- [ ] Добавить Sentry для error tracking
- [ ] Настроить Dependabot

### Спринт 6 — TypeScript миграция

- [ ] Мигрировать `src/services/` на TypeScript
- [ ] Мигрировать `src/utils/` на TypeScript
- [ ] Мигрировать `src/components/` на TypeScript
- [ ] Включить strict mode
- [ ] Добавить typecheck в CI
